def emit(
    job_id: str,
    event_type: str,
    agent: str | None = None,
    tier_used: int | None = None,
    message: str = "",
    data: dict | None = None,
):
    # Placeholder implementation for emit
    pass


def get_events_since(job_id: str, since: int = 0) -> list[dict]:
    # Placeholder implementation for get_events_since
    return []
